% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate_core.R
\name{populate_core}
\alias{populate_core}
\title{Populate Core Variables Across Datasets}
\usage{
populate_core(mighty_metadata_list)
}
\arguments{
\item{mighty_metadata_list}{A list of metadata objects, where each element
contains dataset information including columns and metadata.}
}
\value{
A modified version of \code{mighty_metadata_list} where datasets marked
with \code{usecore = TRUE} have core variables from supplier datasets added as
predecessor columns.
}
\description{
Identifies datasets that supply core variables and adds them as predecessors
to datasets that use core variables.
}
\details{
The function performs the following steps:
\itemize{
\item Identifies datasets that supply core variables
\item Verifies core variables are unique across datasets (stops if duplicates found)
\item Identifies datasets that use core variables (via \code{usecore} metadata flag)
\item Adds missing core variables as predecessor columns to datasets that use them
}
}
\examples{
\dontrun{
updated_metadata <- populate_core(mighty_metadata_list)
}

}
