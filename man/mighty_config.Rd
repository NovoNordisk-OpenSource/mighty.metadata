% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mighty_config.R, R/y_write_config.R
\name{mighty_config}
\alias{mighty_config}
\title{Mighty Config}
\usage{
mighty_config(path)
}
\arguments{
\item{path}{\code{character(1)} path to a directory containing a \verb{_mighty.yml}
file.}
}
\value{
A \code{mighty_config} S7 object extending \link[S7schema:S7schema]{S7schema::S7schema}.
\describe{
\item{\code{external_data}}{A list of external data source specifications,
each with an \code{id} and \code{keys} field.}
}
}
\description{
\code{mighty_config()} provides a robust way of working with the \verb{_mighty.yml}
configuration file in the \code{{mighty}} framework.

A new object is initialized by supplying a directory path containing a
\verb{_mighty.yml} file. The file is automatically validated against the
\code{mighty.json} schema when loaded.

\code{mighty_config()} inherits from \code{S7schema::S7schema()}. You can validate
an object at any time by calling \code{validate()} and use \code{write_config()} to
save it back as a yaml file.
}
\details{
The \verb{_mighty.yml} file is validated against the \code{mighty.json} schema on
load. The file must contain an \code{external_data} array declaring the primary
keys of any datasets external to the ADaM study (e.g. SDTM or reference
datasets) that ADaM domain specifications may depend on.
}
\section{Write Config}{

Use \code{write_config()} to serialize a \code{mighty_config()} object back to a
\verb{_mighty.yml} file.

Supply \code{path} to write to a specific directory; defaults to the directory
the object was loaded from.
}

\examples{
x <- mighty_config(
  path = system.file("examples", package = "mighty.metadata")
)

# Custom print method gives a small overview
print(x)

# Underlying object is a `list`
str(x)

# Write back to a directory
tmp <- tempdir()
write_config(x, path = tmp)

}
\seealso{
\link{mighty_study}, \link{mighty_domain}, \code{\link[=write_config]{write_config()}}
}
