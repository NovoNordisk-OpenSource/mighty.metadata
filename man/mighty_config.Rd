% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a_mighty_config.R
\name{mighty_config}
\alias{mighty_config}
\title{Mighty Config}
\usage{
mighty_config(file, .data)
}
\arguments{
\item{file}{\code{character(1)} path to a \verb{_mighty.yml} file. Mutually exclusive
with \code{.data}.}

\item{.data}{\code{list} holding a \verb{_mighty.yml} configuration already in memory.
Mutually exclusive with \code{file}. The resulting object has \verb{@file} set to
\code{NULL}, so \code{\link[=write_config]{write_config()}} requires an explicit \code{path}.}
}
\value{
A \code{mighty_config} S7 object extending \link[S7schema:S7schema]{S7schema::S7schema}.
\describe{
\item{\code{external_data}}{A list of external data source specifications,
each with an \code{id} and \code{keys} field.}
\item{\code{repos}}{Optional character vector of component repository
locations, or \code{NULL} if not specified.}
}
}
\description{
\code{mighty_config()} provides a robust way of working with the \verb{_mighty.yml}
configuration file in the \code{{mighty}} framework.

A new object is initialized by supplying either the path to a \verb{_mighty.yml}
file or an in-memory \code{list} of the same content. Both are automatically
validated against the \code{mighty.json} schema.

\code{mighty_config()} inherits from \code{S7schema::S7schema()}. You can validate
an object at any time by calling \code{validate()} and use \code{write_config()} to
save it back as a yaml file.
}
\details{
The \verb{_mighty.yml} file is validated against the \code{mighty.json} schema on
load. The file must contain an \code{external_data} array declaring the primary
keys of any datasets external to the ADaM study (e.g. SDTM or reference
datasets) that ADaM domain specifications may depend on.

The optional \code{repos} field specifies where \code{mighty.component} should look
for shared components. Each entry is either a local path (e.g. \code{"."}) or a
GitHub reference in \code{owner/repo/subdir@ref} format (e.g.
\code{"NovoNordisk-OpenSource/mighty.standards/components@main"}).
}
\section{Write Config}{

Use \code{\link[=write_config]{write_config()}} to serialize a \code{mighty_config()} object back to a
\verb{_mighty.yml} file. Supply \code{path} to write to a specific file; defaults to
the file the object was loaded from.
}

\examples{
x <- mighty_config(
  file = system.file("examples", "_mighty.yml", package = "mighty.metadata")
)

# Custom print method gives a small overview
print(x)

# Underlying object is a `list`
str(x)

# Write back to a file
tmp <- tempfile(fileext = ".yml")
write_config(x, path = tmp)

# Or build one in memory
y <- mighty_config(
  .data = list(
    external_data = list(list(id = "DM", keys = "USUBJID")),
    repos = "."
  )
)

# In-memory objects have no file, so `write_config()` needs a `path`
y@file

}
\seealso{
\link{mighty_study}, \link{study_config}, \link{mighty_domain}, \code{\link[=write_config]{write_config()}}
}
